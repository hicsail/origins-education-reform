from setuptools import setup

setup(
    name             = 'corpus_nlp',
    version          = '0.0.0.1',
    install_requires = ['gensim', 'nltk', 'matplotlib', 'numpy', 'scipy', 'statsmodels'],
    license          = 'MIT',
    url              = 'https://github.com/hicsail/origins-education-reform',
    description      = 'NLP library for large corpora.'
)

